# Changelog

所有重要变更都会记录在此文件中。

## [1.2.0] - 2026-06-28

### 新增

- 新增现代化主页、工具箱、设置页和关于页，优化教学场景下的信息层级。
- 新增侧边栏折叠功能，折叠后仅显示图标，并加入平滑过渡动画。
- 新增页面切换淡入动画和动态页码指示。
- 新增工具箱中的整篇 PDF OCR 识别流程，支持识别进度显示和结果复制。
- 新增根目录单启动器发布包布局：zip 根目录只有 `PdfTeachAnnotator.exe`，完整应用在 `App/`，文档在 `docs/`，诊断工具在 `tools/`。

### 变更

- `main` 分支保持 Tesseract-only：OCR 默认使用本地 `Tesseract 5.2.0`，支持中文简体和英文。
- PaddleOCR 实验方案已保留到 `feature/paddleocr` 分支，不再作为 `main` 分支默认能力。
- README 已重写为当前功能口径，修正 PDF 渲染、OCR、快捷键、批注保存和打包说明。
- 关于页文案已更新为 `PdfTeachAnnotator` 品牌，突出课堂批注、旁路保存和本地 OCR。
- 发布包不再依赖根目录散落 DLL 或 `.lnk` 快捷方式，用户解压后双击根目录 `PdfTeachAnnotator.exe` 即可运行。

### 修复

- 修复主页“浏览文件”按钮点击无效的问题。
- 修复侧边栏折叠后宽度过窄导致图标被遮挡的问题。
- 修复首次打开文档时页码显示为 `第 1 页 / 共 0 页` 的问题。
- 修复深浅色主题切换时因修改冻结画刷导致的启动或运行异常。
- 修复缺失右箭头图标资源导致应用无法启动的问题。

### 技术

- 移除 `main` 分支中的 IronOcr 依赖与服务入口。
- 移除 `main` 分支中的 PaddleOCR 预留服务入口，避免与 Tesseract-only 发布口径冲突。
- OCR 服务工厂固定创建 `TesseractOcrService`。
- GitHub Actions 发布流程更新为稳定的 `App/` 完整应用目录 + 根启动器 zip 布局。

## [1.1.1] - 2026-05-24

### 修复

- 修复应用图标路径问题。

## [1.1.0] - 2026-05-24

### 新增

- 添加 OCR 文字识别功能。
